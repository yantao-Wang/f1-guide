# 开发环境配置档案

本文件记录 `走近围场 · F1咨讯` 在开发机（macOS / Apple Silicon）上的完整环境配置，用于换机还原与协作交接。
**存档日期：2026-09-08**。敏感信息（私钥、token）不在此文件中，仅记录配置方式与验证命令。

## 1. 工具链

| 工具 | 版本 | 安装方式 | 用途 |
| --- | --- | --- | --- |
| Go | 1.27.1 darwin/arm64 | `brew install go` | 编译与测试 |
| gh | 2.100.0 | `brew install gh` | GitHub 仓库操作 |
| colima | 0.10.3 | `brew install colima` | macOS 上的 Docker 运行时（Linux VM） |
| docker | 29.8.0 | `brew install docker` | Docker CLI（连 colima 里的守护进程） |
| docker-compose | 5.5.1 | `brew install docker-compose` | 编排（注意：**不是** `docker compose` 插件） |
| golangci-lint | 最新 | `make tools` → `~/go/bin` | 静态分析 |
| govulncheck | 最新 | `make tools` → `~/go/bin` | 依赖漏洞扫描 |

## 2. Go 模块网络

国内直连 proxy.golang.org 会超时，已持久配置中国镜像（写入 Go 全局配置，重装 Go 后需重设）：

```bash
go env -w GOPROXY=https://goproxy.cn,direct
go env GOPROXY   # 验证
```

缓存位置（活跃工作资产，勿随意删除）：GOCACHE=`~/Library/Caches/go-build`、GOMODCACHE=`~/go/pkg/mod`、GOBIN=`~/go/bin`。

## 3. Git / GitHub 访问

HTTPS 推送被墙，改用 **SSH-over-443**：

- `~/.ssh/config` 配置：

  ```
  Host github.com
      HostName ssh.github.com
      Port 443
      User git
  ```

- SSH 公钥已添加到 GitHub 账号（`gh ssh-key add` 或网页手动添加），验证：`ssh -T git@github.com`
- `gh auth login` 的 token 存在 **macOS 钥匙串**（keyring），账号 `yantao-Wang`，scopes：`repo`、`admin:public_key`、`gist`、`read:org`，验证：`gh auth status`
- 仓库本地 git 身份（仅对本仓库生效）：

  ```bash
  git config user.name yantao-Wang
  git config user.email 78951228+yantao-Wang@users.noreply.github.com
  git config http.version HTTP/1.1   # 规避 HTTPS 传输抖动
  ```

## 4. 容器与数据库

- Docker 守护进程运行在 **colima VM** 内（macOS 原生虚拟化框架，aarch64）：
  - 启动：`colima start`；停止：`colima stop`
  - VM 磁盘在 `~/.colima/_lima/colima/disk`（约 1.8G，与下载缓存无关）
- PostgreSQL 容器 `f1-db-1`（`postgres:17-alpine`），由 `docker-compose.yml` 定义：
  - 端口：`localhost:5432`
  - 账号/密码/库名：`f1guide` / `f1guide` / `f1guide`（仅本地开发用）
  - 日常管理：`make docker-up` / `make docker-down`
- 初始化：`make migrate-up`（迁移文件嵌入二进制，随仓库走）+ `make seed`（幂等示例数据）

## 5. 应用环境变量（前缀 `F1GUIDE_`，config 包自动绑定）

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `F1GUIDE_HTTP_ADDR` | `:8080` | 服务监听地址 |
| `F1GUIDE_DATABASE_URL` | `postgres://f1guide:f1guide@localhost:5432/f1guide?sslmode=disable` | 数据库连接 |
| `F1GUIDE_LOG_LEVEL` | `info` | 日志级别 |
| `F1GUIDE_ADMIN_PASSWORD` | 无（后台禁用） | 管理后台登录密码；设置后启用 /admin |
| `F1GUIDE_ADMIN_SECRET` | 随机（重启失效） | 会话 Cookie 签名密钥，生产建议显式设置 32+ 字节随机串 |
| `F1GUIDE_UPLOAD_DIR` | `./uploads` | 后台上传文件存储目录（Docker 部署挂卷） |
| `F1GUIDE_TEST_DATABASE_URL` | 无（未设置时集成测试跳过） | 集成测试数据库 |
| `F1API_BASE_URL` | `https://api.jolpi.ca` | 赛事数据上游地址，联调可指向本地代理 |

## 6. 本机代理（既有环境，非本项目配置）

本机代理 `127.0.0.1:7897` 为开发机既有环境，用于境外下载限速时加速（如 Colima 镜像 GitHub CDN 直连仅 ~28KB/s）：

```bash
HTTPS_PROXY=http://127.0.0.1:7897 HTTP_PROXY=http://127.0.0.1:7897 <命令>
# 例：HTTPS_PROXY=http://127.0.0.1:7897 colima start
```

端口按本机代理软件实际配置调整。

## 7. 换机还原清单

1. `brew install go gh colima docker docker-compose`
2. `go env -w GOPROXY=https://goproxy.cn,direct`
3. `colima start` → `cd F1 && make docker-up && make migrate-up && make seed`
4. `make tools`（golangci-lint + govulncheck）
5. 生成 SSH 密钥并加入 GitHub 账号（HTTPS 推送不可用时走 ssh.github.com:443），`gh auth login`
6. 仓库内设置本地 git 身份（见 §3）
7. `make run` 冒烟：`curl localhost:8080/health` 返回 `{"status":"ok"}`

## 8. 验证清单（一次性自检）

```bash
go env GOPROXY                                  # 中国镜像
docker ps                                       # f1-db-1 healthy
ssh -T git@github.com                           # SSH 可用
gh auth status                                  # 已登录、scopes 齐全
export F1GUIDE_TEST_DATABASE_URL=postgres://f1guide:f1guide@localhost:5432/f1guide?sslmode=disable
go test -count=1 -tags=integration -coverpkg=./internal/...,./pkg/... -coverprofile=cover.out ./internal/... ./pkg/... ./tests/...   # 全量测试 + 覆盖率（-count=1 防缓存污染）
```
